<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <Switch
        android:id="@+id/switch1"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="81dp"
        android:textColor="#fff"
        android:textOff="@string/offered"
        android:textOn="@string/required"
        android:thumb="@drawable/rectangle_thumb"
        android:track="@drawable/rectangle_track"
        tools:layout_editor_absoluteX="51dp"
        tools:layout_editor_absoluteY="16dp" />

    <FrameLayout
        android:layout_width="300dp"
        android:layout_height="450dp"

        android:layout_alignStart="@+id/switch1"
        android:layout_below="@+id/switch1"
        android:layout_marginTop="16dp">


        <ListView
            android:id="@+id/listOffered"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:listitem="@layout/item_offered"/>

        <ListView
            android:id="@+id/listRequired"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone"
            />

    </FrameLayout>
</RelativeLayout>